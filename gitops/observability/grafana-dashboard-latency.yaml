apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-latency
  namespace: observability
data:
  thesis-latency.json: |
    {
      "id": null,
      "uid": "thesis-latency",
      "title": "Thesis API Latency",
      "schemaVersion": 39,
      "version": 2,
      "refresh": "10s",
      "panels": [
        {
          "type": "timeseries",
          "title": "Average Latency by Service (ms)",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
          "targets": [
            {
              "expr": "sum by(service) (app_request_duration_ms_sum) / clamp_min(sum by(service) (app_request_duration_ms_count), 1)",
              "legendFormat": "{{service}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "Max Latency by Service (ms)",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
          "targets": [
            {
              "expr": "max by(service) (app_request_duration_ms_max)",
              "legendFormat": "{{service}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "Throughput (req/s) by Service",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
          "targets": [
            {
              "expr": "sum by(service) (rate(app_requests_total[1m]))",
              "legendFormat": "{{service}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "Error Rate (4xx+5xx) by Service",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
          "targets": [
            {
              "expr": "sum by(service) (rate(app_requests_status_total{status_code=~\"4..|5..\"}[1m]))",
              "legendFormat": "{{service}}"
            }
          ]
        }
      ],
      "templating": { "list": [] }
    }
